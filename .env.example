# 端末間同期 (ADR 0064)。どちらも公開前提の値で、ビルド時にクライアントへ埋め込まれる。
# 未設定でもビルドは通り、同期 UI が非表示になるだけ。
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# migration 適用用。ローカル限定・CI には渡さない (ADR 0064 決定 9)
# DATABASE_URL=postgresql://postgres:<パスワード>@db.<ref>.supabase.co:5432/postgres
