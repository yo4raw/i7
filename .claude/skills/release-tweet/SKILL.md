---
name: release-tweet
description: >-
  i7マネ部屋（IDOLiSH7 衣装DB / https://i7.yo4raw.com）のリリース内容を告知する
  日本語ツイート（X投稿文）を作成するスキル。最新のリリースタグの差分（コミット/PR/ADR）から
  変更点を読み取り、ファン向けの親しみやすい告知文を3案生成する。
  「リリースした内容をツイートにして」「リリース告知のツイートを作って」「今回の更新を
  Xに投稿したい」「アプデ内容を告知して」など、リリース・アップデートの告知文/ツイート/X投稿
  を求められたら必ずこのスキルを使うこと。バージョンを指定された場合（例: v1.37.0の告知）も対象。
  作成した告知文は、本文をプリフィルした X 投稿用リンク（intent リンク）として出力し、
  ユーザーがクリックして投稿画面を開き、内容を確認して手動投稿できるようにする。
  ブラウザ自動操作や認証情報による自動投稿は行わない。
---

# リリース告知ツイート作成

i7マネ部屋のリリース内容を、ファンが読んで「使ってみよう」と思える日本語ツイートに変換する。

## このスキルがすること

最新（または指定）のリリースタグの変更点を git から読み取り、要点を絞った告知ツイートを **3案** 出す。アイドリッシュセブンのプレイヤー（=主な利用者）に向けた、親しみやすく分かりやすい文体にする。

そのうえで、本文をプリフィルした **X 投稿用リンク（`https://x.com/intent/post`）を生成**し、ユーザーがクリック1つで投稿画面を開いて内容を確認し、手動で投稿できるようにする（手順4）。**自動投稿は行わない**（ブラウザ自動操作・X の認証情報は使わない）。

## 1. リリース内容を集める

ユーザーがバージョンを指定しなければ最新タグを対象にする。

```bash
# 最新2つのタグを取得（最新リリースと、その1つ前）
git tag --sort=-creatordate | head -2
# 例: v1.37.0 / v1.36.0 が返る → 直前タグ..最新タグ の差分を見る
git log v1.36.0..v1.37.0 --format='%s'
```

- バージョン指定がある場合はその範囲（`<前タグ>..<指定タグ>`）を使う。
- コミット件名（`feat:` / `fix:` 等）が変更点の一次情報。`feat:`/`fix:` が告知価値の高い変更。`docs:`/`chore:`/`Merge` は基本的に告知に含めない。
- 件名だけで意図が分かりにくい場合は、対応する ADR（`docs/adr/`）や spec（`docs/superpowers/specs/`）の冒頭を読んで背景を補う。
- 内部用語はユーザー可視語に直す（プロジェクト方針）。「カード」→**「衣装」**、共有ブローチ→**「共通ブローチ」**。

変更点が複数あるときは、ファンにとって一番うれしい1〜2点に主役を絞る。全部を詰め込むと読まれない。

## 2. ツイートを書く

### 文体・トーン
- アイナナのプレイヤーに向けた、親しみやすく明るい口調。堅い「リリースしました」より「〜できるようになりました！」「〜が便利になりました🎉」のような体験ベースの言い回し。
- 絵文字は2〜4個程度で雰囲気づくりに使う（🎉✨🎵🎴📊📷 など機能に合うもの）。盛りすぎない。
- 誇大表現（「最強」「完璧」等）は避け、何が便利になったかを具体的に伝える。

### 構成の目安
1. 何が新しく/便利になったか（主役の変更点）
2. （任意）一言の補足や使いどころ
3. ハッシュタグ ＋ サイトURL

### ハッシュタグ・URL
- 末尾に `#アイドリッシュセブン` `#アイナナ` から1〜2個 ＋ `https://i7.yo4raw.com` を付ける。
- ツールの通称「i7マネ部屋」を本文で触れてよい（タグにはしない）。

### 文字数（重要）
Xの上限は280。**日本語は1文字=2、URLは長さに関係なく23** として数える。つまり実質「全角約140字」が上限で、URL(23)とハッシュタグの分を引くと本文は概ね **全角110字前後まで**。各案は必ずこの枠に収め、超えそうなら要点を削る。3案は長さに変化をつける（短め／標準／やや詳しめ）と選びやすい。

## 3. 3案を提示する

次の形式で、文字数（全角換算の概算）を添えて出す。ユーザーが選んで微調整できるようにする。

```
【案1（短め）】
<本文> #アイナナ https://i7.yo4raw.com
（約NN字 / 全角換算）

【案2（標準）】
...

【案3（やや詳しめ）】
...
```

3案はトーンや切り口を少しずつ変える（例: 案1=機能ズバリ、案2=使う場面の提案、案3=丁寧な紹介）。同じ文の言い換えだけにしない。

## 例

**Input**（v1.37.0 の差分）: `feat: スコア計算の編成・スコアを画像(PNG)で共有`

**Output（案の一例）**:
```
スコア計算の編成＆スコアが画像で保存できるようになりました📷✨
推し編成のスクショをそのままシェアできます！
#アイナナ #アイドリッシュセブン https://i7.yo4raw.com
```
（約95字 / 全角換算）

## 4. 投稿用リンクを生成する（自動投稿はしない）

3案を提示したら、**標準スタイル（案2相当）の告知文について、本文をプリフィルした X 投稿用リンクを生成**して提示する。ユーザーはそのリンクをクリックすると本文入りの投稿画面が開くので、内容を確認して自分で「ポスト」する。

- 自動投稿（ブラウザ自動操作・X へのログイン）は行わない。`.env` の `X_ID`/`X_PASS` も読まない・使わない。
- 既定では案2（標準）のリンクを出す。ユーザーが別の案や修正版を選んだら、その文面でリンクを生成し直す。

### 4-1. リンク形式

X の Web Intent を使う。本文全体（ハッシュタグ・URL を含む）を **URL エンコード**して `text` パラメータに渡す。

```
https://x.com/intent/post?text=<URLエンコードした本文>
```

### 4-2. リンクの作り方

手作業でのエンコードは誤りやすいので、`node` の `encodeURIComponent` で生成する。改行・絵文字・`#`・`?` 等も正しくエスケープされる。

```bash
node -e '
const t = `<ここに最終文面（改行そのまま）>`;
console.log("https://x.com/intent/post?text=" + encodeURIComponent(t));
'
```

### 4-3. 結果報告

最終文面（プレーンテキスト）と、生成した投稿用リンクの両方を提示する。「リンクを開くと本文が入った状態で投稿画面が開くので、確認のうえ手動で投稿してください」と一言添える。

> 補足: Web Intent リンクはログイン済みのブラウザ/アプリで開けば、そのまま本文入りの投稿画面になる。X 側の UI 変更の影響を受けにくく、認証情報も不要で安定している。
