# Development Dockerfile
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Expose port
EXPOSE 3000

# Use hot reload in development
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]